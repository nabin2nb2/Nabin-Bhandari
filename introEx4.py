#4.	Gets the user’s first and last name and outputs them in reverse order with a comma between them.

First_Name = input("Enter First name: ")
Last_name = input ("Enter Last name: ")

print ("First name in reverse order - " + str(list(reversed(First_Name) )))

print ("Last name in reverse order - " + str(list(reversed(Last_name))))

